x = float(input("first num: "))
y = float(input("second num: "))

z = x / y

#using an f string to round 
print(f"{z:.2f}")


# rounding to to the nearest to digits
# z = round(x / y, 2)

#print(z)
# rounding a value
# z = round(x + y)

# add a comma after 3 digits
# print(f"{z:,}")
